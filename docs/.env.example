# Arquivo de Exemplo de Variáveis de Ambiente
#
# Este arquivo serve como um guia para as variáveis de ambiente necessárias
# para rodar o projeto localmente. Copie este arquivo para .env.local
# e preencha com os valores corretos.
#
# As variáveis de ambiente no Netlify devem ser configuradas na UI do site.
# Lembre-se: NUNCA comite arquivos .env ou .env.local no Git.

# URL da API para o ambiente de desenvolvimento local
VITE_API_URL=http://localhost:8000

# Chave publicável do Clerk para autenticação (exemplo)
# Obtenha esta chave no dashboard do Clerk
VITE_CLERK_PUBLISHABLE_KEY=pk_test_xxxxxxxxxxxxxxxxxxxxxxxxxxxxx
