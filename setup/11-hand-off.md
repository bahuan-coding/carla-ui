# 11 - Hand-off / Deploy

Antes de entregar:
- `pnpm lint` e `pnpm build` sem erros.
- `netlify dev` validado (redirect SPA, headers, envs).
- Variáveis em Netlify: `VITE_API_URL`, chaves públicas; segredos fora do código.

Entrega:
- Main -> produção; PR -> Deploy Preview.
- Checklist rápido: chat em tempo real ou fallback, filtros funcionando, gráficos ok, tema toggle ok.

Próximos incrementos:
- Drill-down de procesos.
- Insights automatizados (LLM) no Dashboard.
- Detalhe de transacción e navegação cruzada para conversa vinculada.

